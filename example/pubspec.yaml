name: google_maps_place_picker_mb_demo
description: Google Maps Place Picker - forked MB edition

version: 1.0.0+1

publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  google_maps_place_picker_mb:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
